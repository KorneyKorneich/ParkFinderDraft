import { StyleSheet } from "react-native";
import { SIZES } from "@shared/ui";

export const styles = StyleSheet.create({
    map: {
        width: SIZES.WIDTH,
    },
    button: {
        zIndex: 10,
        position: "absolute",
    },
});
