import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    countryContainer: {
        flexDirection: "row",
        marginVertical: 10,
        gap: 10,
    },
});

export default styles;
